---
name: current_strategy
type: project
description: Rolling 30-min tactical strategy generated by Dream Agent V2.
last_updated: 2026-04-05T09:59:27.697882+00:00
---

We need to produce tactical strategy bullets. Identify contradictions: existing strategy says ignore all position close signals; exclusively open new SOL/USDT positions based on momentum, volume, and confidence triggers. Supermemory feedback says strategy updated to focus exclusively on short positions with specific thresholds: <= -0.5% momentum, >=1.5x volume, >=85% confidence, 2% risk per trade, 0.3% SL, 0.6% TP. Also says ignore position close signals. So no contradiction: both say ignore close signals, open new positions based on triggers. Existing didn't specify short vs long, thresholds. New data adds specifics: short only, thresholds. So resolve by adopting short-only with those thresholds.

Past performance: 0 trades, no PnL, so no data to refine; need to change approach? Since no trades, we can't say worked. So we should maybe adjust to be more aggressive? But we have thresholds. Could consider lowering confidence threshold to increase trades? But risk controller gave those thresholds; we should follow. So tactical strategy: open short SOL/USDT when momentum <= -0.5%, volume >=1.5x 20-period avg, confidence >=85%; risk 2% per trade, SL 0.3%, TP 0.6%; ignore all close signals; ensure no other pairs.

We need 4-6 bullet points. Provide concrete actionable rules.

Let's craft:

- Only consider SOL/USDT short entries; skip any long signals.
- Enter short when 5‑minute momentum ≤ -0.5% AND current volume ≥ 1.5 × 20‑period average volume AND signal confidence ≥ 85%.
- Size each trade to risk exactly 2% of account equity; set stop‑loss at 0.3% below entry and take‑profit at 0.6% above entry (short direction).